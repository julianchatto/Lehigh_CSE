/*
    .h file is a header file for c program
    - contains
        - function signatures
        - can also contatin anything a .c file can 

    header fiels act as a table of contents for a library
    allow us to partially compile object code, even when the function definition is missing
*/

int function_one(int x);

int function_two(int x);